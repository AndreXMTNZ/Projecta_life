import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Projecta_life',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
